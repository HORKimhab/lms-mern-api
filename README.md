
## Create migration

```bash
npm run migration:create --name={NameFileMigration}
# npm run migration:create --name=CreateUserTable
```

- After you create migration, you need to fill sql script to up and down
